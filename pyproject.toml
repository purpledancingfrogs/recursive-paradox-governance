[project]
name = "recursive-paradox-governance"
version = "0.1.0"
description = "Deterministic governance engine conserving paradox as a first-class invariant."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
